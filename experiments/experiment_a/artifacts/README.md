Artifacts store your model training results and metrics.  `./../models` is great for 
storing your trained models, but artifacts is useful for generating plots and tables to
compare your different architectures.